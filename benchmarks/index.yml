tasks:
  - id: example
    title: Example
    before: echo before {{cwd}}
    main: echo main {{input}} {{before}}
    after: echo after {{input}} {{before}}
    inputs: [one, two]

inputs:
  - id: one
    title: One
    value: inputOne
  - id: two
    value: inputTwo

shell: false

variables:
  cwd: pwd
