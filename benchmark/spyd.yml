inputs:
  one: 1
  two: 2
select: not network slow uniform exponential growing
tasks: tasks.js
reporter: [debug, history, histogram, boxplot]
reporterConfig:
  histogram:
    mini: true
  boxplot:
    mini: true
